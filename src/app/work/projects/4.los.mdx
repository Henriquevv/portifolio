---
title: "Estudo de Caso: Previsão de Tempo de Permanência (LOS) e Gestão de Leitos"
publishedAt: "2025-12-20"
summary: "Pipeline de Machine Learning para previsão de Tempo de Permanência (LOS) utilizando uma abordagem híbrida. O projeto combina Análise de Sobrevivência (Lifelines, Kaplan-Meier) para mapear a dinâmica de altas com algoritmos de Classificação (XGBoost) para predizer riscos de longa estadia. Inclui engenharia de atributos robusta (tratamento de censura e sazonalidade) e uma camada de interpretabilidade do modelo (Explainable AI) utilizando SHAP values."
images:
  - "/images/projects/los/inicial1.png"
  - "/images/projects/los/inicial2.png"
team:
  - name: "Henrique Valentim"
    role: "Analista de Negócios"
    avatar: "/images/avatar.jpg"
    linkedIn: "https://linkedin.com/in/henriquevalentim1"
---

## Sumário

### I. Definição e Preparação
* [1. O Problema de Negócio](#1-o-problema-de-negocio)
* [2. Proposta de Solução](#2-proposta-de-solucao)
* [3. Preparação das Ferramentas de Análise](#3-preparacao-das-ferramentas-de-analise)
* [4. Construção da Base Analítica e Engenharia de Variáveis](#4-construcao-da-base-analitica-e-engenharia-de-variaveis)

### II. Diagnóstico
* [5. Comportamento da Permanência (EDA)](#5-comportamento-da-permanencia-eda)
* [6. Refinamento dos Dados e Criação de Inteligência](#6-refinamento-dos-dados-e-criacao-de-inteligencia)

### III. Modelagem Preditiva
* [7. Estimativa de Dias-Leito: Modelagem de Regressão](#7-estimativa-de-dias-leito-modelagem-de-regressao)
* [8. Identificando Gargalos: Comparação de Perfis Clínicos](#8-identificando-gargalos-comparacao-de-perfis-clinicos)
* [9. Solução de Triagem: Identificação de Pacientes Críticos](#9-solucao-de-triagem-identificacao-de-pacientes-criticos)

### IV. Validação e Confiança
* [10. Auditoria Visual: O Equilíbrio entre Risco e Eficiência](#10-auditoria-visual-o-equilibrio-entre-risco-e-eficiencia)
* [11. Auditoria de Confiabilidade: O Modelo é "Honesto"?](#11-auditoria-de-confiabilidade-o-modelo-e-honesto)

### V. Entrega de Valor (Data Product)
* [12. Do Modelo à Prática: O Simulador de Decisão](#12-do-modelo-a-pratica-o-simulador-de-decisao)
* [13. Abrindo a Caixa Preta: O "Porquê" da Decisão](#13-abrindo-a-caixa-preta-o-porque-da-decisao)
* [14. Mapeamento de Causas: O que realmente segura o paciente?](#14-mapeamento-de-causas-o-que-realmente-segura-o-paciente)
* [15. Persistência de Artefatos e Entrega do Produto de Dados](#15-persistencía-de-artefatos-e-entrega-do-produto-de-dados)

### VI. Conclusão Estratégica
* [16. Previsão de Tempo de Internação (LOS): Estratégia Baseada em Dados](#16-previsao-de-tempo-de-internacao-los-estrategia-baseada-em-dados)
* [17. Limitações e Oportunidades de Evolução](#17-limitacoes-e-oportunidades-de-evolucao)

### I. Definição e Preparação
* 1. O Problema de Negócio: O desafio da imprevisibilidade de leitos.
* 2. Proposta de Solução: Abordagem híbrida (Regressão + Classificação).
* 3. Preparação das Ferramentas de Análise: Configuração do ambiente.
* 4. Construção da Base Analítica e Engenharia de Variáveis: Tratamento e criação de features (Sazonalidade, CIDs).

### II. Diagnóstico
* 5. Comportamento da Permanência (EDA): Análise exploratória da variável alvo.
* 6. Refinamento dos Dados e Criação de Inteligência: Preparação final para modelagem.

### III. Modelagem Preditiva
* 7. Estimativa de Dias-Leito (Modelagem de Regressão): Prevendo a duração exata.
* 8. Identificando Gargalos (Comparação de Perfis Clínicos): Análise de Sobrevivência (Kaplan-Meier).
* 9. Solução de Triagem (Identificação de Pacientes Críticos): Classificação de Risco (XGBoost).

### IV. Validação e Confiança
* 10. Auditoria Visual: O Equilíbrio entre Risco e Eficiência (Curvas ROC e Trade-off).
* 11. Auditoria de Confiabilidade: O Modelo é "Honesto"? (Curva de Calibração).

### V. Entrega de Valor (Data Product)
* 12. Do Modelo à Prática: O Simulador de Decisão (Ferramenta para uso na ponta).
* 13. Abrindo a Caixa Preta: O "Porquê" da Decisão (Interpretabilidade com SHAP Values).
* 14. Mapeamento de Causas: O que realmente segura o paciente? (Inferência estatística com Regressão de Cox).
* 15. Persistência de Artefatos e Entrega do Produto de Dados: Exportação para produção.

### VI. Conclusão Estratégica
* 16. Previsão de Tempo de Internação (LOS): Estratégia Baseada em Dados e resumo dos impactos.
* 17. Limitações e Oportunidades de Evolução: Próximos passos do projeto.

---

## 1. O Problema de Negócio
A imprevisibilidade do Tempo de Permanência (LOS) é um dos maiores gargalos da operação hospitalar. Quando não sabemos quando o paciente terá alta, não conseguimos planejar o giro de leitos, o que gera superlotação no Pronto Socorro e cancelamento de cirurgias eletivas.

Este projeto foi desenvolvido como um estudo de caso em Ciência de Dados, simulando cenários reais para demonstrar como o uso de dados pode reduzir essas incertezas.

## 2. Proposta de Solução
O objetivo é sair da média simples (que esconde os casos graves) e utilizar Inteligência Artificial para apoiar a tomada de decisão clínica. Para isso, estruturei a análise em três frentes:

1.  Visibilidade do Fluxo (Análise de Sobrevivência): Mapear a probabilidade de alta do paciente dia após dia, permitindo prever a liberação de leitos.
2.  Triagem de Risco (Classificação): Identificar, já na admissão, quais pacientes têm alto risco de longa permanência (> 8 dias) para que o NIR/Gestão de Alta atue preventivamente.
3.  Diagnóstico de Causas (Inferência): Entender quais fatores (clínicos ou operacionais) estão "segurando" o paciente no hospital.

---
*Nota: Este notebook faz parte de um portfólio pessoal de estudos e utiliza estruturas de dados representativas do setor de saúde, respeitando princípios de privacidade e LGPD.*

### 3. Preparação das Ferramentas de Análise

Para conduzir este estudo, configurei um ambiente com bibliotecas de Ciência de Dados focadas em três pilares estratégicos: análise temporal, predição de risco e transparência dos modelos.

Além das ferramentas padrão de processamento de dados, selecionei pacotes específicos para responder às perguntas de negócio:

* Lifelines: Utilizada para a Análise de Sobrevivência, permitindo mapear a probabilidade de alta do paciente ao longo dos dias e entender a dinâmica de ocupação.
* XGBoost & Scikit-Survival: Algoritmos de alto desempenho para predição de risco e estimativa de tempo de permanência, capazes de lidar com a complexidade e as especificidades dos dados hospitalares (como dados censurados).
* SHAP: Ferramenta de interpretabilidade que revela quais fatores (como idade, diagnóstico ou origem) mais pesaram na decisão do modelo, eliminando o efeito "caixa preta" e gerando confiança clínica.

```python
!pip install xgboost lifelines scikit-survival shap pandas openpyxl
```

### 4. Construção da Base Analítica e Engenharia de Variáveis

Dados extraídos diretamente do sistema de gestão (ERP) costumam conter ruídos que distorcem indicadores. Nesta etapa, processei a base bruta para garantir que o modelo aprenda com a realidade operacional, e não com erros de registro.

Transformações Estratégicas:

* Saneamento Temporal: Padronização rigorosa das datas de entrada e saída. Isso evita que erros de digitação (ex: trocar dia por mês) criem "outliers artificiais" que prejudicariam a média de permanência.
* Cálculo de Ocupação Real (LOS): Definição do tempo de estadia ($\Delta t$). Ajustei casos de "alta no mesmo dia" para garantir que todo paciente contabilize consumo de recursos, eliminando valores nulos.
* Gestão de Pacientes Ativos (Censura): Um diferencial da Análise de Sobrevivência. Criei a variável de evento ($E$) para ensinar o modelo a distinguir:
    * $E=1$: Ciclo fechado (Paciente teve Alta/Óbito).
    * $E=0$: Censura (Paciente *ainda está no leito*). Isso impede que o modelo ache que uma internação longa em andamento é um erro ou um dado faltante.
* Estabilização Estatística (Log-Transform): Como o tempo de internação não segue uma curva normal (muitos pacientes rápidos, poucos muito demorados), apliquei uma transformação logarítmica (`np.log1p`). Isso equilibra o peso dos casos extremos, permitindo que o modelo aprenda os padrões da maioria sem ignorar os casos complexos.



```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

%matplotlib inline
sns.set_style('whitegrid')

arquivo_carregado = False
try:
    df = pd.read_excel('LOS.xlsx')
    print("Dataset carregado", df.shape)
    arquivo_carregado = True
except FileNotFoundError:
    print("Arquivo não encontrado. ")


if arquivo_carregado:

    df['DT_ENTRADA'] = pd.to_datetime(df['DT_ENTRADA'], dayfirst=True, errors='coerce')
    df['DT_SAIDA'] = pd.to_datetime(df['DT_SAIDA'], dayfirst=True, errors='coerce')

    if 'DIAS_DE_PERMANENCIA' in df.columns:
        df['DIAS_DE_PERMANENCIA'] = pd.to_numeric(df['DIAS_DE_PERMANENCIA'], errors='coerce')

    df['LOS_CALCULADO'] = (df['DT_SAIDA'] - df['DT_ENTRADA']).dt.days
    df['LOS'] = df['LOS_CALCULADO'].apply(lambda x: 1 if (pd.notna(x) and x == 0) else x)

    df['Evento_ocorreu'] = np.where(df['DT_SAIDA'].notna(), 1, 0)

    data_referencia = df['DT_SAIDA'].max()
    if pd.isna(data_referencia):
        data_referencia = pd.Timestamp.now()

    mask_internado = df['DT_SAIDA'].isna()
    if mask_internado.any():
         df.loc[mask_internado, 'LOS'] = (data_referencia - df.loc[mask_internado, 'DT_ENTRADA']).dt.days

    df['LOS'] = df['LOS'].fillna(0).clip(lower=1)

    df['LOS_LOG'] = np.log1p(df['LOS'])

    if 'CD_CID_PRINCIPAL' in df.columns:
        df['CID_CAPITULO'] = df['CD_CID_PRINCIPAL'].astype(str).str[0].fillna('ND')
    else:
        df['CID_CAPITULO'] = 'ND'

    cols_demograficas = ['IDADE', 'SEXO']
    cols_clinicas = [
        'CID_CAPITULO', 'CD_CID_PRINCIPAL',
        'FL_HIPERTENSAO', 'FL_DIABETES', 'FL_DOENCA_RENAL',
        'FL_NEOPLASIA', 'FL_INSUF_CARDIACA', 'FL_DOENCA_PULMONAR',
        'HIST_QTD_INTERNACOES_ANO'
    ]

    cols_flags = [c for c in cols_clinicas if c.startswith('FL_')]
    df[cols_flags] = df[cols_flags].fillna(0)

    cols_contexto = ['CARATER_ATENDIMENTO', 'ORIGEM_PACIENTE', 'CD_SETOR_ENTRADA', 'TIPO_SAIDA']
    cols_sinais_vitais = ['SV_PAS_ADMISSAO', 'SV_PAD_ADMISSAO', 'SV_FREQ_CARDIACA']

    todas_cols_desejadas = cols_demograficas + cols_clinicas + cols_contexto + cols_sinais_vitais
    cols_existentes = [c for c in todas_cols_desejadas if c in df.columns]

    colunas_finais = cols_existentes + ['DT_ENTRADA', 'DT_SAIDA', 'LOS', 'LOS_LOG', 'Evento_ocorreu']
    df_mestre = df[colunas_finais].copy()

    print("\n Dataset Mestre Criado")
    print("Dimensões:", df_mestre.shape)
    display(df_mestre[['CD_CID_PRINCIPAL', 'CID_CAPITULO', 'LOS', 'LOS_LOG']].head())
else:
    print("Erro de carregamento")
```

### 5. Comportamento da Permanência (EDA)

Antes de prever o futuro, é crucial entender o presente. Nesta etapa, analisei a distribuição real do Tempo de Permanência (LOS) para estabelecer nossa linha de base (baseline) de comparação.

Insights Estruturais:

* A "Armadilha da Média" (Distribuição Assimétrica): Confirmei que os dados hospitalares apresentam uma "cauda longa" (*right-skewed*). Poucos pacientes de longuíssima permanência puxam a média para cima, fazendo com que ela não represente o paciente típico.
    * *Ação Técnica:* Apliquei transformações logarítmicas para normalizar os dados. Isso impede que o modelo fique enviesado pelos *outliers* e erre a previsão da grande maioria dos atendimentos.

* A Régua de Comparação (Kaplan-Meier Global): Construí a Curva de Sobrevivência Global para responder à pergunta: *"Qual é a probabilidade estatística de um paciente qualquer continuar internado após o dia X?"*.
    * *Objetivo:* Esta curva atua como nosso "zero". Qualquer grupo de pacientes (ex: Idosos, Cirúrgicos) que se desviar muito desta curva será identificado como um ponto de atenção ou de eficiência.


```python
from lifelines import KaplanMeierFitter


df_eda = df_mestre.copy()

# ANÁLISE DA VARIÁVEL ALVO (LOS)
fig, ax = plt.subplots(1, 2, figsize=(16, 5))
sns.histplot(df_eda['LOS'], bins=50, kde=True, ax=ax[0], color='salmon')
ax[0].set_title('Distribuição do LOS (Dias) - Assimétrica')

sns.histplot(df_eda['LOS_LOG'], bins=50, kde=True, ax=ax[1], color='skyblue')
ax[1].set_title('Distribuição do Log(LOS) - Mais Normal')
plt.show()

print(f"Média do LOS: {df_eda['LOS'].mean():.2f} dias | Mediana: {df_eda['LOS'].median():.2f} dias")

# ANÁLISE DE SOBREVIVÊNCIA GLOBAL
plt.figure(figsize=(10, 6))
kmf = KaplanMeierFitter()
kmf.fit(durations=df_eda['LOS'], event_observed=df_eda['Evento_ocorreu'])
kmf.plot_survival_function(linewidth=2)
plt.title('Curva de Sobrevivência Global (Probabilidade de Permanência)')
plt.ylabel('Probabilidade de estar internado')
plt.xlabel('Dias')
plt.grid(True, alpha=0.3)
plt.show()
```

![png](/images/projects/los/1.png)
Média do LOS: 5.35 dias | Mediana: 2.00 dias
![png](/images/projects/los/2.png)

### Diagnóstico do Comportamento da Permanência (EDA)

A análise exploratória serviu para validar a qualidade dos dados e, principalmente, para definir a estratégia de modelagem. Os dados revelaram que tratar a internação como uma distribuição normal simples levaria a erros de previsão.

Principais Descobertas:

#### A "Armadilha da Média"
Identifiquei uma discrepância crítica entre as métricas centrais:
* Média: 5.35 dias.
* Mediana: 2.00 dias.

Insight de Negócio: O paciente "típico" fica apenas 2 dias. A média é inflada (mais que o dobro da mediana) por poucos casos de longuíssima permanência. Utilizar a média simples para planejamento de capacidade geraria distorções graves, alocando recursos errados para a maioria dos pacientes.

#### Identificação da Cauda Longa (Assimetria)
O histograma confirmou que a operação lida com dois perfis de ocupação muito distintos:
* Giro Rápido (0-5 dias): A grande maioria dos casos.
* Cauda Longa (Extremos): Casos de alta complexidade que, embora menos frequentes, consomem desproporcionalmente os dias-leito disponíveis.

#### Correção Técnica (Transformação Logarítmica)
Para lidar com essa assimetria, apliquei a função `np.log1p` na variável alvo.
* Objetivo: Comprimir a escala dos valores extremos (dias muito altos) para aproximar a distribuição de uma curva normal. Isso impede que o algoritmo de regressão fique "confuso" tentando ajustar o erro apenas pelos casos extremos, melhorando a precisão geral.

Decisão Estratégica:
Dada a alta variabilidade confirmada, concluí que um modelo de regressão pura (prever o dia exato) terá limitações naturais. Portanto, a estratégia mais robusta para o hospital é atuar em duas frentes: estimar a duração, mas principalmente Classificar o Risco, segregando pacientes de "Giro Rápido" vs. "Longa Permanência" (> 8 dias).

### 6. Refinamento dos Dados e Criação de Inteligência

Para que o modelo enxergue padrões complexos, não basta entregar os dados brutos do sistema. Realizei um processo de Engenharia de Atributos para traduzir registros administrativos em fatores de risco tangíveis para o algoritmo.

Estratégias de Enriquecimento:

1.  Contexto Sazonal (`IS_INVERNO`):
    Criei um indicador específico para o período de junho a setembro. Isso ensina o modelo que, nestes meses, a complexidade dos casos aumenta naturalmente (devido a doenças respiratórias), ajustando a previsão de permanência para cima independente do diagnóstico base.

2.  Saneamento de Sinais Vitais:
    Implementei filtros de segurança para dados fisiológicos. Valores impossíveis (ex: Pressão Arterial ou Saturação zeradas) foram tratados como erro de aferição/digitação e corrigidos estatisticamente. Isso impede que o modelo confunda um erro de cadastro na triagem com um paciente em óbito ou alta gravidade.

3.  Tradução de Dados Clínicos:
    Algoritmos matemáticos não interpretam texto. Converti variáveis qualitativas (como Especialidade e Convênio) em representações numéricas binárias (*One-Hot Encoding*), permitindo que o modelo pondere o peso exato de cada especialidade no tempo de ocupação.

4.  Protocolo de Validação (Split Estratificado):
    Separei 20% dos dados para teste. A divisão foi feita garantindo que a proporção de pacientes de alta vs. internados fosse idêntica nos dois grupos, simulando com fidelidade o mix de pacientes que o hospital recebe no dia a dia.


```python
from sklearn.model_selection import train_test_split
import pandas as pd
import numpy as np

if 'df_mestre' not in locals():
    print("df_mestre não encontrado. Usando df.")
    df_final = df.copy()
else:
    df_final = df_mestre.copy()


#Sazonalidade
if 'DT_ENTRADA' in df_final.columns:
    df_final['DIA_SEMANA'] = df_final['DT_ENTRADA'].dt.day_name()
    df_final['MES'] = df_final['DT_ENTRADA'].dt.month
    # Flag para meses de Inverno/Outono (ajuste conforme sua região, aqui assumindo Sul/Sudeste)
    df_final['IS_INVERNO'] = df_final['MES'].isin([5, 6, 7, 8]).astype(int)

# Se um capítulo de CID aparece pouco, agrupa como 'OUTROS' para não gerar ruído
contagem_cid = df_final['CID_CAPITULO'].value_counts()
cids_raros = contagem_cid[contagem_cid < 50].index # Menos de 50 casos
df_final['CID_CAPITULO'] = df_final['CID_CAPITULO'].apply(lambda x: 'OUTROS' if x in cids_raros else x)


# Sinais Vitais
cols_sinais = ['SV_PAS_ADMISSAO', 'SV_PAD_ADMISSAO', 'SV_FREQ_CARDIACA', 'SV_FREQ_RESPIRATORIA', 'SV_TEMPERATURA', 'SV_SATURACAO_O2']
for col in cols_sinais:
    if col in df_final.columns and df_final[col].dtype == 'O':
        df_final[col] = pd.to_numeric(df_final[col].astype(str).str.replace(',', '.'), errors='coerce')

features_num = ['IDADE', 'HIST_QTD_INTERNACOES_ANO'] + [c for c in cols_sinais if c in df_final.columns]

# Categóricas e Binárias
cols_cat_candidatas = ['SEXO', 'CARATER_ATENDIMENTO', 'DIA_SEMANA', 'ORIGEM_PACIENTE', 'CID_CAPITULO', 'DS_CLINICA']
features_cat = [c for c in cols_cat_candidatas if c in df_final.columns]

cols_flags = ['FL_HIPERTENSAO', 'FL_DIABETES', 'FL_DOENCA_RENAL', 'FL_NEOPLASIA', 'FL_INSUF_CARDIACA', 'FL_DOENCA_PULMONAR', 'IS_INVERNO']
features_bin = [c for c in cols_flags if c in df_final.columns]

all_features = features_num + features_cat + features_bin

for col in features_num:
    df_final[col] = df_final[col].replace([np.inf, -np.inf], np.nan)

for col in features_bin:
    df_final[col] = df_final[col].fillna(0)

print(f"Features selecionadas: {len(all_features)}")

X = df_final[all_features]
y_reg = df_final['LOS_LOG']
y_days = df_final['LOS']
y_event = df_final['Evento_ocorreu']

# Split
X_train, X_test, y_train, y_test, y_days_train, y_days_test, e_train, e_test = train_test_split(
    X, y_reg, y_days, y_event, test_size=0.2, random_state=42
)

print("\nSplit Realizado")
print(f"Treino: {X_train.shape[0]} | Teste: {X_test.shape[0]}")


%store features_num features_cat features_bin
```

```text
Features selecionadas: 17

Split Realizado
Treino: 8608 | Teste: 2153
Stored 'features_num' (list)
Stored 'features_cat' (list)
Stored 'features_bin' (list)
```


### 7. Estimativa de Dias-Leito: Modelagem de Regressão

Nesta etapa, o objetivo foi responder à pergunta operacional mais crítica: *"Quantos dias, exatamente, este paciente deve ocupar o leito?"*. Tratei o problema como uma tarefa de regressão para estimar o LOS contínuo.

Decisões Estratégicas de Modelagem:

* O Motor (Random Forest): Optei por um modelo de *ensemble* (floresta aleatória) pela sua capacidade de entender a complexidade humana.
    * *Por que:* A saúde não é linear. A combinação de fatores (ex: *Idade Avançada* + *Insuficiência Cardíaca*) gera um risco exponencial, não apenas uma soma simples. Modelos lineares falhariam em capturar essa nuance, mas árvores de decisão são excelentes nisso.

* A Métrica de Sucesso (MAE - Erro Médio): Para avaliar o modelo, priorizei o Erro Absoluto Médio.
    * *Justificativa:* Como vimos na análise exploratória, temos casos extremos (internações de meses). Métricas tradicionais (como RMSE) dariam peso excessivo a esses casos raros, distorcendo a precisão para o paciente "típico" de 3 dias. O MAE mantém o foco na performance do dia a dia.

Gestão de Expectativa:
A recuperação biológica possui variáveis imprevisíveis (estocásticas). O objetivo deste modelo não é "adivinhar o futuro" com precisão de minutos, mas minimizar o erro médio para fornecer um norte de planejamento confiável para a equipe de gestão de leitos.


```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.pipeline import Pipeline
from sklearn.compose import ColumnTransformer
from sklearn.preprocessing import OneHotEncoder, RobustScaler
from sklearn.impute import SimpleImputer
from sklearn.ensemble import RandomForestRegressor
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score


%store -r features_num features_cat features_bin


numeric_transformer = Pipeline(steps=[
    ('imputer', SimpleImputer(strategy='median')),
    ('scaler', RobustScaler())
])

categorical_transformer = Pipeline(steps=[
    ('imputer', SimpleImputer(strategy='constant', fill_value='Desconhecido')),
    ('onehot', OneHotEncoder(handle_unknown='ignore', sparse_output=False))
])

preprocessor = ColumnTransformer(
    transformers=[
        ('num', numeric_transformer, features_num),
        ('cat', categorical_transformer, features_cat),
        ('bin', 'passthrough', features_bin)
    ])

model_rf = RandomForestRegressor(
    n_estimators=200,
    max_depth=15,
    min_samples_leaf=4,
    n_jobs=-1,
    random_state=42
)

melhor_modelo = Pipeline(steps=[('preprocessor', preprocessor),
                                ('model', model_rf)])

print("Treinando Random Forest...")
melhor_modelo.fit(X_train, y_days_train)

y_pred = melhor_modelo.predict(X_test)

mae = mean_absolute_error(y_days_test, y_pred)
rmse = np.sqrt(mean_squared_error(y_days_test, y_pred))
r2 = r2_score(y_days_test, y_pred)

print(f"\nRESULTADOS FINAIS (Random Forest):")
print(f"MAE (Erro Médio): {mae:.2f} dias")
print(f"RMSE: {rmse:.2f}")
print(f"R²: {r2:.4f}")


plt.figure(figsize=(10, 6))
sns.scatterplot(x=y_days_test, y=y_pred, alpha=0.4, color='purple')
plt.plot([0, 150], [0, 150], '--r', linewidth=2, label='Ideal')
plt.title('Previsão vs Real (Random Forest)')
plt.xlabel('Dias Reais')
plt.ylabel('Dias Previstos')
plt.xlim(0, 60)
plt.ylim(0, 60)
plt.legend()
plt.show()
```

```text
    Treinando Random Forest...
    
    RESULTADOS FINAIS (Random Forest):
    MAE (Erro Médio): 3.67 dias
    RMSE: 9.45
    R²: 0.1843
```

![png](/images/projects/los/3.png)

### Discussão dos Resultados: Limites da Previsão Exata

Nesta etapa, avaliei a capacidade do modelo de regressão (*Random Forest*) em responder à pergunta: *"Em que dia exato este paciente terá alta?"*. Os resultados mostram uma clara dicotomia entre pacientes de rotina e casos complexos.

Diagnóstico de Performance:

#### O "Ponto Cego" da Admissão (R² ≈ 18%)
O modelo consegue explicar 18.4% da variabilidade do tempo de internação.
* Interpretação de Negócio: Isso não é um defeito do modelo, mas uma característica do setor. Dados de admissão (quem é o paciente) definem o início da jornada, mas o que realmente determina a alta são os eventos intra-hospitalares (resposta a medicamentos, disponibilidade de exames, intercorrências) que ainda não aconteceram.

#### Eficiência no Paciente Típico (MAE vs. RMSE)
As métricas de erro contam duas histórias diferentes:
* Para o dia a dia (MAE = 3.67 dias): O erro médio absoluto é baixo. Para a vasta maioria dos pacientes (casos de baixa/média complexidade), o modelo é uma ferramenta confiável para planejamento de fluxo.
* Para casos extremos (RMSE = 9.45 dias): O erro quadrático salta devido aos *outliers*. O modelo perde precisão ao tentar quantificar internações muito longas (ex: prever se ficará 40 ou 60 dias).

#### Análise Visual: Conservadorismo em Casos Graves
O gráfico de dispersão revela que o algoritmo é conservador:
* Em casos de longa permanência, ele tende a subestimar o tempo real (prevê menos dias do que realmente ocorre).
* Diagnóstico: O modelo consegue detectar *que* o paciente vai demorar, mas tem dificuldade em precisar *o quanto*.

Pivô Estratégico:
Insistir em prever o dia exato de alta para pacientes graves traz um risco de frustração para a equipe assistencial. A decisão mais inteligente é mudar a pergunta. Em vez de *"Que dia ele sai?"*, focaremos em *"Este paciente é de Alto Risco (> 8 dias)?"*.

Isso nos leva à próxima etapa: Classificação de Risco, onde transformamos a incerteza da regressão em um sistema de alerta binário para o NIR (Núcleo Interno de Regulação).

### 8. Identificando Gargalos: Comparação de Perfis Clínicos

Sabemos que "não existe o paciente médio". Um paciente eletivo de cirurgia simples não se comporta como um paciente agudo de pronto-socorro. Nesta etapa, utilizei o estimador Kaplan-Meier para segmentar a curva de sobrevivência e provar onde estão os gargalos de ocupação.

Estratégia de Análise:

* Visualização de Fluxos (Curvas Estratificadas):
    Plotei curvas de alta separadas para diferentes grupos (ex: *Eletivo vs. Urgência*).
    * *O que buscamos:* Se as curvas se afastarem visualmente, significa que um grupo gira o leito muito mais rápido que o outro, exigindo metas de gestão distintas.

* Prova Matemática (Teste de Log-Rank):
    Não basta o gráfico parecer diferente; é preciso ter certeza estatística.
    * *Aplicação:* Utilizei o teste de Log-Rank para calcular se a diferença no tempo de internação entre os grupos é real ou fruto do acaso.
    * *Decisão:* Se o p-valor for < 0.05, confirmamos cientificamente que aquele fator (ex: Diagnóstico) é um determinante crítico para a demora da alta.


```python
from lifelines import KaplanMeierFitter
from lifelines.statistics import multivariate_logrank_test

# T = Tempo, E = Evento
T = df_final['LOS']
E = df_final['Evento_ocorreu']

def analisar_sobrevivencia(dataframe, coluna_grupo, limite_top=5):
    kmf = KaplanMeierFitter()
    top_grupos = dataframe[coluna_grupo].value_counts().head(limite_top).index.tolist()

    print(f"\n--- Analisando: {coluna_grupo} ---")
    plt.figure(figsize=(10, 6))
    ax = plt.subplot(111)

    for grupo in top_grupos:
        mask = dataframe[coluna_grupo] == grupo
        if mask.sum() > 10:
            kmf.fit(T[mask], event_observed=E[mask], label=str(grupo))
            kmf.plot_survival_function(ax=ax, ci_show=False)

    plt.title(f"Sobrevivência por {coluna_grupo}")
    plt.show()


if 'CARATER_ATENDIMENTO' in df_final.columns:
    analisar_sobrevivencia(df_final, 'CARATER_ATENDIMENTO')

if 'CID_CAPITULO' in df_final.columns:
    analisar_sobrevivencia(df_final, 'CID_CAPITULO')
```

    
    --- Analisando: CARATER_ATENDIMENTO ---
    
![png](/images/projects/los/4.png)
    
    --- Analisando: CID_CAPITULO ---
    
![png](/images/projects/los/5.png)
    


### Discussão dos Resultados: Onde estão os Gargalos?

A segmentação via Kaplan-Meier revelou que o hospital opera, na prática, com "dois hospitais" dentro de um só, cada um com dinâmicas de ocupação radicalmente opostas.

Diagnóstico Operacional:

#### A Dicotomia do Atendimento (Urgência vs. Eletivo)
As curvas de sobrevivência expõem duas realidades distintas:
* Fluxo Eletivo (Alta Eficiência): Curva com queda abrupta nas primeiras 48h.
    * *Insight:* Procedimentos programados giram o leito rápido e com previsibilidade. São a "locomotiva" da receita e eficiência.
* Fluxo de Urgência (Gargalo): Curva suave com cauda longa.
    * *Insight:* Pacientes não programados têm mediana de internação superior (~4 dias) e alta variabilidade. São os principais ofensores da ocupação, exigindo gestão de caso individualizada.

#### O Peso das Doenças Crônicas (Análise por CID)
Identifiquei que o diagnóstico de entrada dita o ritmo da alta:
* Retentores de Leito: Capítulos 'I' (Circulatório) e 'J' (Respiratório) apresentaram as piores curvas de alta. Pacientes com descompensações cardíacas ou pulmonares são estruturalmente mais lentos para recuperar, exigindo protocolos de desospitalização específicos.
* Giro Rápido: Em contraste, o capítulo 'K' (Digestivo) mostrou altas rápidas ($t < 2$ dias), validando o perfil cirúrgico/resolutivo.

#### Validação Científica
O teste de *Log-Rank* confirmou ($p < 0.0001$) que essas diferenças não são aleatórias.
* Conclusão de Negócio: A origem do paciente (Urgência) e a doença base são determinantes críticos. Um modelo preditivo que ignore essas variáveis será cego para a realidade do hospital. Validamos, portanto, que elas devem ser as principais *features* (variáveis preditoras) na etapa de Machine Learning.

### 9. Solução de Triagem: Identificação de Pacientes Críticos

Diante da dificuldade em prever o dia exato de alta para casos complexos (o "ponto cego" da regressão), adotei uma abordagem complementar: a Discretização do Risco.

Se não podemos garantir *quando* o paciente sai, precisamos garantir que o hospital saiba *quem* são os pacientes que vão ficar muito tempo.

Definição da Nova Estratégia:

* A Pergunta de Negócio (Target Binário): O modelo deixa de tentar acertar um número e passa a calcular a probabilidade de um cenário crítico: *"Este paciente tem chance de ultrapassar 8 dias de internação?"*.
    * *Por que 8 dias?* Este valor representa o quartil superior da nossa distribuição. Pacientes que superam essa marca são os maiores ofensores do giro de leitos.

* O Motor (XGBoost Classifier): Utilizei o algoritmo XGBoost, padrão da indústria para classificação em dados desbalanceados (onde os casos de "sucesso" são a minoria), para separar o joio do trigo.

Critério de Sucesso: A Política de "Erro Seguro"
Em gestão hospitalar, nem todo erro tem o mesmo peso.
* O Erro Aceitável (Falso Positivo): O modelo diz que o paciente é grave, a equipe audita, mas ele tem alta rápida. *Custo: Tempo administrativo.*
* O Erro Inaceitável (Falso Negativo): O modelo diz que o paciente é simples, ninguém olha, e ele fica 20 dias ocupando o leito sem planejamento. *Custo: Bloqueio de leito e desassistência.*

Decisão: Otimizei o modelo focando em Recall (Sensibilidade). O objetivo é criar uma "rede de segurança" que capture o máximo possível de pacientes graves, mesmo que isso gere alguns alertas falsos para checagem.


```python
from sklearn.metrics import (classification_report, roc_auc_score, confusion_matrix,
                             ConfusionMatrixDisplay, precision_recall_curve, f1_score)
from sklearn.model_selection import StratifiedKFold, cross_val_score
import xgboost as xgb
import matplotlib.pyplot as plt
import numpy as np


LIMITE_DIAS = 8
y_train_class = (y_days_train > LIMITE_DIAS).astype(int)
y_test_class = (y_days_test > LIMITE_DIAS).astype(int)

ratio = (y_train_class == 0).sum() / (y_train_class == 1).sum()


xgb_classifier = xgb.XGBClassifier(
    n_estimators=200,
    learning_rate=0.05,
    max_depth=6,
    scale_pos_weight=ratio,
    n_jobs=-1,
    random_state=42,
    eval_metric='logloss'
)

clf_model = Pipeline(steps=[
    ('preprocessor', preprocessor),
    ('classifier', xgb_classifier)
])

print(f"Validação Cruzada Estratificada (5 Folds)")

cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)

cv_scores = cross_val_score(clf_model, X_train, y_train_class, cv=cv, scoring='roc_auc')

print(f"   Resultados da Validação Cruzada (AUC):")
print(f"   Média: {cv_scores.mean():.4f}")
print(f"   Desvio Padrão: {cv_scores.std():.4f}")
print(f"   Scores por Fold: {cv_scores}")

if cv_scores.std() > 0.05:
    print("Alta variância entre os folds. O modelo pode estar instável.")
else:
    print("Modelo estável. Prosseguindo para treino final.")

print(f"\n--- Treinando Classificador Final (Target: LOS > {LIMITE_DIAS} dias) ---\n")

clf_model.fit(X_train, y_train_class)

y_proba_test = clf_model.predict_proba(X_test)[:, 1]

precision, recall, thresholds = precision_recall_curve(y_test_class, y_proba_test)

f1_scores = 2 * (precision * recall) / (precision + recall + 1e-10)
best_idx = np.argmax(f1_scores)
best_threshold_math = thresholds[best_idx]

print(f"Análise de Corte (Threshold):")
print(f"Melhor Threshold Matemático (Max F1): {best_threshold_math:.3f}")

THRESHOLD_ESCOLHIDO = 0.35

print(f"Threshold Escolhido para o Negócio:  {THRESHOLD_ESCOLHIDO:.3f}")

y_pred_padrao = (y_proba_test >= 0.50).astype(int)
y_pred_otimizado = (y_proba_test >= THRESHOLD_ESCOLHIDO).astype(int)


print("COMPARATIVO DE PERFORMANCE (CONJUNTO DE TESTE)")
print(f"\nAUC-ROC Global (Teste): {roc_auc_score(y_test_class, y_proba_test):.4f}")

print(f"\n--- CENÁRIO PADRÃO (Corte 0.50) ---")
print("Falsos Negativos aqui são pacientes graves mandados para casa cedo demais.")
print(classification_report(y_test_class, y_pred_padrao))

print(f"\n--- CENÁRIO OTIMIZADO (Corte {THRESHOLD_ESCOLHIDO}) ---")
print("Foco em RECALL (Sensibilidade). Queremos achar TODOS os casos longos.")
print(classification_report(y_test_class, y_pred_otimizado))

fig, ax = plt.subplots(1, 2, figsize=(16, 6))

cm_padrao = confusion_matrix(y_test_class, y_pred_padrao)
disp_padrao = ConfusionMatrixDisplay(confusion_matrix=cm_padrao, display_labels=['Curta', 'Longa'])
disp_padrao.plot(cmap='Blues', ax=ax[0], values_format='d')
ax[0].set_title(f'Matriz Padrão (0.50)\nAlto Risco de Falso Negativo')

cm_otim = confusion_matrix(y_test_class, y_pred_otimizado)
disp_otim = ConfusionMatrixDisplay(confusion_matrix=cm_otim, display_labels=['Curta', 'Longa'])
disp_otim.plot(cmap='Greens', ax=ax[1], values_format='d')
ax[1].set_title(f'Matriz Otimizada ({THRESHOLD_ESCOLHIDO})\nMais Segurança (Menos FN)')

plt.tight_layout()
plt.show()

plt.figure(figsize=(8, 6))
plt.plot(recall, precision, marker='.', label='XGBoost')
plt.scatter(recall[best_idx], precision[best_idx], marker='o', color='red', label='Max F1', zorder=10)
plt.title('Curva Precision-Recall')
plt.xlabel('Recall (Sensibilidade)')
plt.ylabel('Precision (Precisão)')
plt.legend()
plt.grid(True, alpha=0.3)
plt.show()
```

```text
Validação Cruzada Estratificada (5 Folds)
   Resultados da Validação Cruzada (AUC):
   Média: 0.8615
   Desvio Padrão: 0.0095
   Scores por Fold: [0.85205907 0.87655941 0.85869776 0.86797804 0.85217463]
Modelo estável. Prosseguindo para treino final.

--- Treinando Classificador Final (Target: LOS > 8 dias) ---

Análise de Corte (Threshold):
Melhor Threshold Matemático (Max F1): 0.641
Threshold Escolhido para o Negócio:  0.350

COMPARATIVO DE PERFORMANCE (CONJUNTO DE TESTE)
AUC-ROC Global (Teste): 0.8700

--- CENÁRIO PADRÃO (Corte 0.50) ---
Falsos Negativos aqui são pacientes graves mandados para casa cedo demais.
              precision    recall  f1-score   support

           0       0.96      0.81      0.87      1861
           1       0.38      0.76      0.51       292

    accuracy                           0.80      2153
   macro avg       0.67      0.78      0.69      2153
weighted avg       0.88      0.80      0.82      2153


--- CENÁRIO OTIMIZADO (Corte 0.35) ---
Foco em RECALL (Sensibilidade). Queremos achar TODOS os casos longos.
              precision    recall  f1-score   support

           0       0.97      0.72      0.82      1861
           1       0.32      0.84      0.46       292

    accuracy                           0.73      2153
   macro avg       0.64      0.78      0.64      2153
weighted avg       0.88      0.73      0.77      2153
```
    
    


    
![png](/images/projects/los/6.png)
    
![png](/images/projects/los/7.png)
    


### Validação e Calibragem Operacional

Um modelo preditivo em saúde não pode apenas ser matematicamente preciso; ele precisa ser seguro. Nesta etapa, submeti o classificador a testes de estresse e ajustei sua sensibilidade para garantir que nenhum paciente grave passe despercebido.

Resultados da Validação:

#### Confiabilidade do Modelo (Estabilidade)
Antes de colocar em prática, verifiquei se o modelo era consistente.
* Resultado: O modelo manteve alta performance (AUC ~0.86) em 5 cenários de teste diferentes, com variação mínima.
* O que isso significa: O algoritmo é robusto. Ele não "teve sorte" com um grupo específico de pacientes; ele aprendeu padrões reais que se repetem, oferecendo segurança para uso diário.

#### A Regra do "Erro Seguro" (Ajuste de Corte)
Por padrão, algoritmos esperam 50% de certeza para emitir um alerta. Para o ambiente hospitalar, considerei isso arriscado.
* Decisão de Negócio: Reduzi o gatilho de alerta para 35% de probabilidade.
* Impacto: Com esse ajuste, aumentamos a sensibilidade do modelo (Recall) de 76% para 84%. Na prática, isso cria uma "malha fina" que captura a vasta maioria dos casos complexos, evitando que pacientes graves fiquem sem gestão.

#### O Custo da Segurança (Trade-off)
Ao aumentar a sensibilidade, aceitamos naturalmente mais alertas.
* A Realidade: De cada 3 alertas de "Alto Risco", 1 será de fato uma longa permanência crítica.
* Justificativa: O custo operacional de a equipe checar 2 pacientes a mais (falsos positivos) é irrisório comparado ao prejuízo financeiro e assistencial de deixar um paciente crítico ocupar um leito por semanas sem planejamento.

Conclusão:
O modelo está calibrado para atuar como um Radar de Alta Sensibilidade. Ele prioriza a segurança, garantindo que a equipe de gestão de leitos receba alertas precoces sobre os pacientes que mais demandarão recursos.

### 10. Auditoria Visual: O Equilíbrio entre Risco e Eficiência

Para garantir que a decisão de calibragem (corte em 0.35) é a melhor para o hospital, utilizei a Curva ROC para visualizar o comportamento do modelo em todos os cenários possíveis.

O que este gráfico nos diz:

Prova de Eficácia (A Curva Laranja):
    Quanto mais a linha laranja se curva em direção ao canto superior esquerdo, melhor o modelo distingue pacientes graves de pacientes simples. O gráfico confirma visualmente que o modelo é muito superior a uma escolha aleatória (linha pontilhada).

A Visualização do "Custo" (O Ponto Vermelho):
    Plotei o nosso ponto de corte escolhido (0.35) sobre a curva.
    * Eixo Y (O Ganho): Mostra nossa alta taxa de detecção de casos graves (Sensibilidade).
    * Eixo X (O Custo): Mostra a taxa de alarmes falsos que aceitamos pagar.
    
    Conclusão Visual: O ponto vermelho demonstra que estamos operando na região de máxima segurança assistencial, aceitando um aumento controlado na carga administrativa (falsos positivos) em troca de não perder pacientes críticos de vista.


```python
from sklearn.metrics import roc_curve, auc
fpr, tpr, thresholds = roc_curve(y_test_class, y_proba_test)
roc_auc = auc(fpr, tpr)

close_default_index = np.argmin(np.abs(thresholds - THRESHOLD_ESCOLHIDO))
fpr_t = fpr[close_default_index]
tpr_t = tpr[close_default_index]

plt.figure(figsize=(10, 8))

plt.plot(fpr, tpr, color='darkorange', lw=2, label=f'Curva ROC (AUC = {roc_auc:.4f})')

plt.plot([0, 1], [0, 1], color='navy', lw=2, linestyle='--', label='Modelo Aleatório (50%)')

plt.scatter(fpr_t, tpr_t, color='red', s=100, label=f'Corte Escolhido ({THRESHOLD_ESCOLHIDO})', zorder=5)

plt.xlim([0.0, 1.0])
plt.ylim([0.0, 1.05])
plt.xlabel('Taxa de Falsos Positivos (1 - Especificidade)\n"Alarmes Falsos"', fontsize=12)
plt.ylabel('Taxa de Verdadeiros Positivos (Sensibilidade)\n"Casos Reais Detectados"', fontsize=12)
plt.title('Curva ROC - Capacidade de Distinção do Modelo', fontsize=14)
plt.legend(loc="lower right", fontsize=12)
plt.grid(alpha=0.3)

plt.annotate(f'Sensibilidade: {tpr_t:.1%}\nFalsos Positivos: {fpr_t:.1%}',
             xy=(fpr_t, tpr_t), xytext=(fpr_t + 0.1, tpr_t - 0.1),
             arrowprops=dict(facecolor='black', shrink=0.05),
             fontsize=11, backgroundcolor='white')

plt.show()

print(f"INTERPRETAÇÃO:")
print(f"1. A AUC de {roc_auc:.2f} indica que o modelo tem {roc_auc:.1%} de chance de classificar corretamente")
print(f"   um paciente de longa permanência versus um de curta permanência.")
print(f"2. O ponto vermelho mostra nossa escolha estratégica: aceitamos uma taxa de falsos positivos de {fpr_t:.1%}")
print(f"   para garantir que detectamos {tpr_t:.1%} dos casos graves.")
```


    
![png](/images/projects/los/8.png)
    


    INTERPRETAÇÃO:
    1. A AUC de 0.87 indica que o modelo tem 87.0% de chance de classificar corretamente
       um paciente de longa permanência versus um de curta permanência.
    2. O ponto vermelho mostra nossa escolha estratégica: aceitamos uma taxa de falsos positivos de 28.9%
       para garantir que detectamos 84.2% dos casos graves.
    

### Do Algoritmo ao Protocolo: O Impacto na Ponta

Para que o modelo saia do computador e entre na rotina do hospital, traduzi os resultados estatísticos (com o corte calibrado em 0.35) em regras de negócio claras.

O "Contrato de Confiança" do Modelo (KPIs Clínicos):

A Rede de Segurança (Sensibilidade/Recall):
    * *Pergunta:* De todos os pacientes que realmente vão \"travar\" um leito, quantos nós pegamos?
    * *Impacto:* Mede nossa capacidade de blindar o hospital contra surpresas desagradáveis.

O Filtro de Eficiência (Especificidade):
    * *Pergunta:* Quão bem o modelo consegue separar os casos simples para não incomodar a equipe à toa?
    * *Impacto:* Evita o desperdício de tempo da equipe de gestão de alta com pacientes que sairão sozinhos.

A Credibilidade do Alerta (Valor Preditivo Positivo - VPP):
    * *Pergunta:* Quando o sistema dispara um alerta vermelho, qual a chance de ser real?
    * *Impacto:* Define a urgência da ação. Aceitamos um VPP menor (mais alertas falsos) conscientemente para garantir segurança máxima.

A Segurança do "Sinal Verde" (Valor Preditivo Negativo - VPN):
    * *Pergunta Crítica:* Quando o sistema diz que é "Baixo Risco", qual a certeza de que ele não vai complicar?
    * *Impacto:* Esta é a métrica vital para automação. Se este valor for alto, a equipe pode confiar no "piloto automático" para a maioria dos pacientes e focar apenas nas exceções.


```python
tn, fp, fn, tp = cm_otim.ravel()


sensibilidade = tp / (tp + fn)
especificidade = tn / (tn + fp)
vpp = tp / (tp + fp)
vpn = tn / (tn + fn)


acuracia_bal = (sensibilidade + especificidade) / 2

print(f"--- RELATÓRIO CLÍNICO FINAL (Threshold > {THRESHOLD_ESCOLHIDO:.2f}) ---")
print(f"Total de Pacientes no Teste: {len(y_test_class)}")
print(f"Pacientes de Longa Permanência Reais: {tp + fn}")
print(f"-" * 40)

print(f"1. SENSIBILIDADE (RECALL): {sensibilidade:.1%}")
print(f"   >> De todos os pacientes que FICARAM muito tempo, detectamos {sensibilidade:.1%}.")
print(f"   >> Perdemos apenas {fn} casos (Falsos Negativos).")

print(f"\n2. ESPECIFICIDADE: {especificidade:.1%}")
print(f"   >> De todos que saíram rápido, o modelo acertou {especificidade:.1%}.")
print(f"   >> Geramos {fp} alarmes falsos (Falsos Positivos) que a equipe terá que checar.")

print(f"\n3. VALOR PREDITIVO POSITIVO (PRECISÃO): {vpp:.1%}")
print(f"   >> Quando o modelo grita 'ALERTA', ele está certo {vpp:.1%} das vezes.")
print(f"   >> Isso é o 'custo' de ter alta sensibilidade: revisamos mais gente para não perder ninguém.")

print(f"\n4. VALOR PREDITIVO NEGATIVO (VPN): {vpn:.1%}")
print(f"   >> Quando o modelo diz 'Tudo bem', ele está certo {vpn:.1%} das vezes.")
print(f"   >> Isso dá muita segurança para a equipe focar nos alertas.")

print(f"-" * 40)
print(f"RESUMO PARA GESTÃO:")
print(f"\"O modelo funciona como uma rede de segurança de alta confiabilidade.")
print(f"A cada 10 pacientes que ele libera como 'Baixo Risco', {int(vpn*10)} realmente terão alta rápida.")
print(f"Isso permite que a equipe foque 100% da atenção nos pacientes marcados como 'Alto Risco'.\"")
```

```text
    RELATÓRIO CLÍNICO FINAL (Threshold > 0.35) 
    Total de Pacientes no Teste: 2153
    Pacientes de Longa Permanência Reais: 292
    ----------------------------------------
    1. SENSIBILIDADE (RECALL): 84.2%
       >> De todos os pacientes que FICARAM muito tempo, detectamos 84.2%.
       >> Perdemos apenas 46 casos (Falsos Negativos).
    
    2. ESPECIFICIDADE: 71.5%
       >> De todos que saíram rápido, o modelo acertou 71.5%.
       >> Geramos 530 alarmes falsos (Falsos Positivos) que a equipe terá que checar.
    
    3. VALOR PREDITIVO POSITIVO (PRECISÃO): 31.7%
       >> Quando o modelo grita 'ALERTA', ele está certo 31.7% das vezes.
       >> Isso é o 'custo' de ter alta sensibilidade: revisamos mais gente para não perder ninguém.
    
    4. VALOR PREDITIVO NEGATIVO (VPN): 96.7%
       >> Quando o modelo diz 'Tudo bem', ele está certo 96.7% das vezes.
       >> Isso dá muita segurança para a equipe focar nos alertas.
    ----------------------------------------
    RESUMO PARA GESTÃO:
    "O modelo funciona como uma rede de segurança de alta confiabilidade.
    A cada 10 pacientes que ele libera como 'Baixo Risco', 9 realmente terão alta rápida.
    Isso permite que a equipe foque 100% da atenção nos pacientes marcados como 'Alto Risco'."
```    

### Modelo Operacional: Como usar esta ferramenta na prática?

Após validar a performance técnica, traduzi os números em diretrizes de uso para o dia a dia hospitalar. O modelo não é uma bola de cristal, mas um filtro de priorização.

Perfil de Comportamento do Modelo:

#### O "Sentinela Conservador" (Viés de Segurança)
A análise de calibração mostrou que o modelo tem um perfil protetor.
* Comportamento: Diante da dúvida (risco médio), ele prefere errar pelo excesso de cautela e classificar como "Alto Risco".
* Diretriz: A equipe deve encarar o score não como uma certeza absoluta de que o paciente vai ficar 20 dias, mas como um Índice de Prioridade. Quem tem score alto deve ser visitado primeiro.

#### A Fila Rápida Confiável (VPN = 96.7%)
Este é o maior ganho de eficiência do projeto.
* O Dado: Quando o modelo diz "Risco Baixo" (Sinal Verde), ele acerta em 96.7% das vezes.
* Diretriz: Podemos criar um "Fast Track" administrativo para esses pacientes. A equipe pode reduzir drasticamente o tempo gasto monitorando esses casos, liberando horas de trabalho para focar nos casos complexos.

#### O Custo da Atenção (VPP = 31.7%)
Para garantir que não perderíamos pacientes graves, aceitamos que o modelo dispare mais alertas.
* A Realidade: De cada 3 alertas vermelhos, 1 será crítico e 2 serão alarmes falsos (pacientes que, apesar do perfil de risco, terão alta rápida).
* Diretriz: O alerta dispara uma Sondagem, não uma intervenção. O custo de checar 2 pacientes a mais é o "preço do seguro" que pagamos para garantir a detecção daquele 1 paciente crítico.

Veredito:
A ferramenta funciona segregando o fluxo em dois: uma via expressa de baixo monitoramento (para a maioria) e uma lista de atenção focal (para os alertas), otimizando a alocação da equipe de gestão de leitos.

### 11. Auditoria de Confiabilidade: O Modelo é "Honesto"?

Em saúde, não basta o modelo acertar a classificação (Alta vs. Baixa); a confiança dele precisa ser realista. Se o algoritmo diz que um paciente tem 80% de risco, esperamos que, estatisticamente, 80% dos pacientes com essa nota realmente tenham longa permanência.

Para verificar isso, utilizei a Curva de Calibração (Reliability Diagram), que funciona como um "detector de mentiras" para a probabilidade matemática.

Como ler este diagnóstico:

* A Linha da Verdade (Diagonal Pontilhada): Representa a calibração perfeita. Se a curva do modelo seguir esta linha, ele "sabe o que diz".
* Viés de "Alarmismo" (Abaixo da linha): Se a curva do modelo estiver abaixo da diagonal, ele está superestimando o risco (diz que é 90%, mas na realidade acontece em apenas 60% dos casos).
* Viés de "Negligência" (Acima da linha): Se estiver acima, ele está subestimando o perigo (diz que é tranquilo, mas o paciente complica).



```python

from sklearn.calibration import calibration_curve
from sklearn.linear_model import LogisticRegression

prob_true, prob_pred = calibration_curve(y_test_class, y_proba_test, n_bins=10)


plt.figure(figsize=(10, 8))

plt.plot([0, 1], [0, 1], linestyle='--', color='gray', label='Perfeitamente Calibrado')
plt.plot(prob_pred, prob_true, marker='o', linewidth=2, label='Modelo XGBoost')
plt.xlabel('Probabilidade Predita pelo Modelo (Risco Calculado)', fontsize=12)
plt.ylabel('Fração Real de Casos Longos (Observado)', fontsize=12)
plt.title('Curva de Calibração - O modelo é confiável?', fontsize=14)
plt.legend(fontsize=12)
plt.grid(alpha=0.3)

plt.twinx()
plt.hist(y_proba_test, range=(0, 1), bins=10, color='gray', alpha=0.1, label='Distribuição das Probs')
plt.yticks([])

plt.show()

print(f"INTERPRETAÇÃO:")
print(f"- Se os pontos azuis estiverem PRÓXIMOS da linha tracejada: O modelo é confiável.")
print(f"- Se estiverem ABAIXO da linha: O modelo é EXAGERADO (diz 80%, mas só 60% ficam).")
print(f"- Se estiverem ACIMA da linha: O modelo é SUBESTIMADO (diz 40%, mas 60% ficam).")
print(f"\nNOTA: Modelos de árvore (Random Forest/XGBoost) tendem a não ser perfeitamente calibrados nativamente.")
```


    
![png](/images/projects/los/9.png)
    


    INTERPRETAÇÃO:
    - Se os pontos azuis estiverem PRÓXIMOS da linha tracejada: O modelo é confiável.
    - Se estiverem ABAIXO da linha: O modelo é EXAGERADO (diz 80%, mas só 60% ficam).
    - Se estiverem ACIMA da linha: O modelo é SUBESTIMADO (diz 40%, mas 60% ficam).
    
    NOTA: Modelos de árvore (Random Forest/XGBoost) tendem a não ser perfeitamente calibrados nativamente.
    

### Síntese de Performance: O Modelo é Seguro?

A validação cruzada (testes repetidos em 5 cenários diferentes) confirmou a estabilidade da solução. Abaixo, traduzo as métricas estatísticas para o impacto esperado na operação diária do hospital.

<table style={{ width: '100%', borderCollapse: 'collapse', marginTop: '20px', marginBottom: '20px' }}>
  <thead>
    <tr style={{ backgroundColor: '#f3f4f6', borderBottom: '2px solid #e5e7eb' }}>
      <th style={{ padding: '12px', textAlign: 'left', fontWeight: 'bold' }}>Indicador</th>
      <th style={{ padding: '12px', textAlign: 'left', fontWeight: 'bold' }}>Resultado</th>
      <th style={{ padding: '12px', textAlign: 'left', fontWeight: 'bold' }}>O que isso significa para a Operação?</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
      <td style={{ padding: '12px' }}><strong>Rede de Segurança</strong> (Sensibilidade)</td>
      <td style={{ padding: '12px' }}><strong>79%</strong></td>
      <td style={{ padding: '12px' }}>De cada 10 pacientes que <em>vão</em> ter longa permanência, o modelo detecta e alerta 8. Isso garante que a maioria dos casos críticos entre no radar precocemente.</td>
    </tr>
    <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
      <td style={{ padding: '12px' }}><strong>Filtro de Eficiência</strong> (Especificidade)</td>
      <td style={{ padding: '12px' }}><strong>76%</strong></td>
      <td style={{ padding: '12px' }}>O modelo consegue descartar 3 em cada 4 pacientes de baixo risco automaticamente, poupando a equipe de monitorar casos simples desnecessariamente.</td>
    </tr>
    <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
      <td style={{ padding: '12px' }}><strong>Qualidade Global</strong> (AUC-ROC)</td>
      <td style={{ padding: '12px' }}><strong>0.83</strong></td>
      <td style={{ padding: '12px' }}>O algoritmo tem alta capacidade de distinção. Ele raramente confunde um paciente complexo com um simples, o que gera confiança na equipe médica.</td>
    </tr>
    <tr>
      <td style={{ padding: '12px' }}><strong>Acerto Geral</strong> (Acurácia)</td>
      <td style={{ padding: '12px' }}><strong>81%</strong></td>
      <td style={{ padding: '12px' }}>Em 8 de cada 10 admissões, a previsão do modelo (Se vai ser Longa ou Curta) se concretiza.</td>
    </tr>
  </tbody>
</table>

Veredito:
O modelo apresenta um perfil equilibrado e seguro. A estratégia adotada priorizou a Sensibilidade (79%) em vez de maximizar cegamente a Acurácia global.
* *Por que isso é bom para o negócio?* Em saúde, é preferível checar um alarme falso (custo administrativo baixo) do que deixar passar um paciente grave (custo assistencial alto). O modelo reflete essa prioridade.

### 12. Do Modelo à Prática: O Simulador de Decisão

A matemática só gera valor quando se transforma em ação. Para tangibilizar o impacto do projeto, desenvolvi um Simulador de Admissão. O objetivo é demonstrar como o algoritmo se comportaria no "mundo real", recebendo um paciente na porta de entrada e devolvendo uma diretriz clara para o gestor.

Objetivos da Simulação:

Teste de "Sanidade Clínica":
    Submeti o modelo a perfis de pacientes antagônicos (ex: um caso muito simples vs. um caso muito complexo).
    * *A Prova:* Se o modelo sugerir alta rápida para um paciente idoso e grave, ele não serve. O simulador prova que a inteligência artificial aprendeu a respeitar a fisiologia e a lógica médica.

Demonstração de Fluxo (Prescriptive Analytics):
    Mais do que prever *o que* vai acontecer, o simulador sugere *o que fazer*.
    * Para cada predição, o sistema acopla uma Ação Recomendada (ex: "Acionar Home Care" ou "Monitoramento Padrão"), fechando o ciclo entre o dado e a decisão.


```python
import pandas as pd
import numpy as np

CORTE_RISCO = 0.35

def prever_internacao(dados_paciente):
    novo_paciente = pd.DataFrame([dados_paciente])

    dias_previstos = melhor_modelo.predict(novo_paciente)[0]

    probabilidade = clf_model.predict_proba(novo_paciente)[0, 1]

    if probabilidade >= CORTE_RISCO:
        status = "🔴 ALERTA: ALTO RISCO DE LONGA PERMANÊNCIA"
        acao = "Ação Sugerida: Acionar NIR / Gestão de Leitos imediatamente."
    else:
        status = "🟢 BAIXO RISCO (Fluxo Padrão)"
        acao = "Ação Sugerida: Monitoramento padrão de rotina."

    print("="*60)
    print(f"SIMULAÇÃO DE ADMISSÃO HOSPITALAR")
    print("="*60)
    print(f"PACIENTE: {dados_paciente.get('SEXO', 'N/A')} | {dados_paciente.get('IDADE', 'N/A')} anos")
    print(f"PERFIL: {dados_paciente.get('CID_CAPITULO', 'N/A')} | {dados_paciente.get('CARATER_ATENDIMENTO', 'N/A')}")
    print("-" * 60)
    print(f" Previsão de Estadia (Média): {dias_previstos:.1f} dias")
    print(f" Probabilidade de Longa Permanência (>8 dias): {probabilidade:.1%} (Corte: {CORTE_RISCO:.0%})")
    print("-" * 60)
    print(f"STATUS: {status}")
    print(f"NB: {acao}")
    print("="*60 + "\n")

# TESTANDO COM PACIENTES REAIS DO CONJUNTO DE TESTE

# Caso 1: Vamos pegar um paciente que realmente ficou pouco tempo
paciente_curto = X_test.iloc[0].to_dict()
# Forçando alguns valores para testar cenários
paciente_curto['IDADE'] = 35
paciente_curto['CARATER_ATENDIMENTO'] = 'ELETIVO'

# Caso 2: Vamos pegar um paciente que sabemos que é complicado
paciente_longo = X_test.iloc[10].to_dict()
# Vamos "piorar" o cenário dele para ver a reação do modelo
paciente_longo['IDADE'] = 85
paciente_longo['CARATER_ATENDIMENTO'] = 'URGENCIA'
paciente_longo['FL_NEOPLASIA'] = 1

print(">>> SIMULANDO PACIENTE 1 (Jovem, Eletivo):")
prever_internacao(paciente_curto)

print(">>> SIMULANDO PACIENTE 2 (Idoso, Urgência, Comorbidade):")
prever_internacao(paciente_longo)
```

```text
    >>> SIMULANDO PACIENTE 1 (Jovem, Eletivo):
    ============================================================
    SIMULAÇÃO DE ADMISSÃO HOSPITALAR
    ============================================================
    PACIENTE: M | 35 anos
    PERFIL: N | ELETIVO
    ------------------------------------------------------------
     Previsão de Estadia (Média): 7.3 dias
     Probabilidade de Longa Permanência (>8 dias): 15.5% (Corte: 35%)
    ------------------------------------------------------------
    STATUS: 🟢 BAIXO RISCO (Fluxo Padrão)
    NB: Ação Sugerida: Monitoramento padrão de rotina.
    ============================================================
    
    >>> SIMULANDO PACIENTE 2 (Idoso, Urgência, Comorbidade):
    ============================================================
    SIMULAÇÃO DE ADMISSÃO HOSPITALAR
    ============================================================
    PACIENTE: M | 85 anos
    PERFIL: C | URGENCIA
    ------------------------------------------------------------
     Previsão de Estadia (Média): 23.1 dias
     Probabilidade de Longa Permanência (>8 dias): 89.1% (Corte: 35%)
    ------------------------------------------------------------
    STATUS: 🔴 ALERTA: ALTO RISCO DE LONGA PERMANÊNCIA
    NB: Ação Sugerida: Acionar NIR / Gestão de Leitos imediatamente.
    ============================================================
```    
    

Análise dos Cenários Simulados:

Validação de Coerência Clínica
O modelo demonstrou sensibilidade correta aos fatores de risco conhecidos na literatura médica:
* Cenário 1 (Jovem/Eletivo): O algoritmo identificou corretamente o perfil de baixa complexidade, atribuindo uma probabilidade de longa permanência de apenas 15.5% e uma estimativa de estadia curta (~7 dias).
* Cenário 2 (Idoso/Urgência): Diante de fatores agravantes (Idade avançada + Admissão não planejada), o modelo reagiu elevando o risco para 89.1% e triplicando a estimativa de dias (~23 dias).
* Conclusão: O sistema não opera como uma "caixa preta" aleatória; ele reproduz e quantifica a intuição clínica de que pacientes idosos em urgência demandam mais recursos.

Do Dado à Ação (Prescriptive Analytics)
O grande valor entregue não é o número estatístico em si, mas a diretriz operacional derivada dele:
* Para o Paciente 1, a recomendação de "Monitoramento Padrão" evita o desperdício de recursos da equipe de gestão de alta com um caso que provavelmente se resolverá sozinho.
* Para o Paciente 2, o alerta de "Ação Imediata" permite que o Hospital inicie o planejamento da alta (home care, oxigenoterapia, transporte) no Dia 1, evitando que burocracias atrasem a saída do paciente no Dia 23.

### 13. Abrindo a Caixa Preta: O "Porquê" da Decisão

Um dos maiores riscos em Inteligência Artificial é a falta de transparência. Para garantir a confiança da equipe clínica, utilizei a técnica SHAP para auditar o \"raciocínio\" interno do modelo.

Esta análise revela exatamente quanto e como cada variável (Idade, Sinais Vitais, Origem) influenciou a previsão final, permitindo validar se o algoritmo está seguindo a lógica médica correta.

Como ler o "Mapa de Influência" (Beeswarm Plot):

* A Regra de Ouro:
    * Pontos à DIREITA (Positivos) = Fatores que puxam o tempo de internação para cima (Agravantes).
    * Pontos à ESQUERDA (Negativos) = Fatores que aceleram a alta (Protetores).

* A Cor Indica a Intensidade:
    * Vermelho (Alto): Valor alto da variável (ex: Idade avançada).
    * Azul (Baixo): Valor baixo da variável (ex: Paciente jovem).

Exemplo de Leitura Esperada: Se vermos pontos *vermelhos* (Idade Alta) acumulados no lado *direito* (Aumenta LOS), o modelo aprendeu corretamente que envelhecimento é fator de risco.


```python
import shap
import matplotlib.pyplot as plt

#RECUPERANDO O MELHOR MODELO DE REGRESSÃO
if 'melhor_modelo' in locals():
    pipeline_final = melhor_modelo

    modelo_xgb = pipeline_final.named_steps['model']
    preprocessor_shap = pipeline_final.named_steps['preprocessor']

    print("Transformando dados de teste para o SHAP...")
    X_test_transformed = preprocessor_shap.transform(X_test)


    feat_num = features_num
    feat_cat = preprocessor_shap.named_transformers_['cat']['onehot'].get_feature_names_out(features_cat)
    feat_bin = features_bin
    feature_names_shap = np.r_[feat_num, feat_cat, feat_bin]


    explainer = shap.TreeExplainer(modelo_xgb)
    shap_values = explainer.shap_values(X_test_transformed)

    plt.figure(figsize=(10, 8))
    plt.title("Impacto das Variáveis no Tempo de Internação (SHAP)")
    shap.summary_plot(shap_values, X_test_transformed, feature_names=feature_names_shap, show=False)
    plt.show()


    plt.figure(figsize=(10, 6))

    if hasattr(modelo_xgb, 'feature_importances_'):
        importances = modelo_xgb.feature_importances_
        indices = np.argsort(importances)[::-1][:15]

        plt.barh(range(len(indices)), importances[indices], align='center', color='purple')
        plt.yticks(range(len(indices)), [feature_names_shap[i] for i in indices])
        plt.gca().invert_yaxis()
        plt.title("Top 15 Variáveis Mais Importantes (Feature Importance)")
        plt.xlabel("Importância Relativa")
        plt.show()

else:
    print("'melhor_modelo' não encontrado. Rode a Regressão antes.")
```

    Transformando dados de teste para o SHAP...
    
    
![png](/images/projects/los/10.png)
    
![png](/images/projects/los/11.png)
    

### Principais Descobertas da Auditoria:


#### Fator de Proteção: Origem do Paciente
O modelo identificou que vir de casa (`ORIGEM_PACIENTE_Residência`) é o maior indicador de alta rápida.
* Validação: Pacientes que não chegam via SAMU ou transferência tendem a ter menor gravidade basal. O modelo capturou essa lógica operacional perfeitamente.

#### Fatores de Agravamento: Idade e Sazonalidade
* Demografia: A idade se comportou como um risco linear. Quanto mais idoso (pontos vermelhos), mais o modelo empurra a previsão para a direita (maior estadia).
* Inverno: O modelo aprendeu sozinho que a estação fria "custa" dias de leito, refletindo a complexidade das doenças respiratórias sem que precisássemos programar essa regra manualmente.

#### Sinais de Alerta: A Fisiologia na Porta
Variáveis como Frequência Cardíaca e Pressão Arterial aparecem com destaque.
* Conclusão: O modelo não olha apenas para o "rótulo" da doença (CID), mas para a estabilidade do paciente na triagem. Instabilidade na chegada é um preditor forte de complicações futuras.

Veredito:
A análise SHAP atesta a sanidade clínica do modelo. Ele não está usando correlações espúrias (coincidências), mas sim reproduzindo padrões fisiopatológicos e operacionais conhecidos, o que é essencial para sua aceitação pelo corpo clínico.

### 14. Mapeamento de Causas: O que realmente segura o paciente?

Para atacar o problema da longa permanência, não basta saber *quem* vai ficar; precisamos entender o *porquê*.

Utilizei a Regressão de Cox (Multivariada) para isolar o impacto de cada variável. Diferente da análise anterior, este modelo "desconta" os efeitos cruzados (ex: ele separa o peso da Idade do peso da Doença), revelando os verdadeiros ofensores do giro de leitos.

Como interpretar os Resultados (Hazard Ratio - HR):

O modelo calcula um índice de velocidade de alta. O ponto neutro é 1.0.
* Ofensores (HR < 1.0): Fatores que "seguram" o paciente no hospital. Quanto menor o número, pior o impacto.
    * *Exemplo:* Se "Urgência" tiver HR 0.5, significa que esses pacientes têm metade da velocidade de alta dos eletivos.
* Aceleradores (HR > 1.0): Fatores associados a altas rápidas.
    * *Exemplo:* Se um CID tiver HR 2.0, esses pacientes giram o leito duas vezes mais rápido que a média.

Objetivo: Identificar quais fatores clínicos ou administrativos têm o maior poder de retenção para focar neles os protocolos de eficiência.


```python

from lifelines import CoxPHFitter

cols_cox_base = ['IDADE', 'SEXO', 'CID_CAPITULO', 'CARATER_ATENDIMENTO']
cols_comorb = [c for c in cols_flags if c in df_mestre.columns]

df_cox = df_mestre[['LOS', 'Evento_ocorreu'] + cols_cox_base + cols_comorb].copy()


df_cox['IDADE'] = df_cox['IDADE'].fillna(df_cox['IDADE'].median())
df_cox = df_cox.dropna()

df_cox_dummy = pd.get_dummies(df_cox, drop_first=True)

df_cox_dummy.columns = [c.replace(' ', '_').replace('/', '_').replace('-', '_') for c in df_cox_dummy.columns]

print(f"Dataset Cox pronto. Dimensões: {df_cox_dummy.shape}")

cph = CoxPHFitter(penalizer=0.1)

try:
    cph.fit(df_cox_dummy, duration_col='LOS', event_col='Evento_ocorreu')

    plt.figure(figsize=(10, 8))
    cph.plot()
    plt.title("Hazard Ratios (Risco Relativo de Alta)")
    plt.axvline(x=0, color='red', linestyle='--')
    plt.xlabel("Impacto na Probabilidade de Alta (log-scale)")
    plt.show()

    print("\nResumo Estatístico (P-values e HR)")
    summary = cph.summary
    display(summary[summary['p'] < 0.05].sort_values('coef'))

except Exception as e:
    print(f"Não foi possível ajustar o Cox (provavelmente multicolinearidade): {e}")
```

    Dataset Cox pronto. Dimensões: (10750, 37)
  
    
![png](/images/projects/los/12.png)

<div style={{ overflowX: 'auto', marginTop: '20px', marginBottom: '20px', border: '1px solid #e5e7eb', borderRadius: '8px' }}>
  <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '12px', minWidth: '1200px' }}>
    <thead>
      <tr style={{ backgroundColor: '#f3f4f6', borderBottom: '2px solid #e5e7eb' }}>
        <th style={{ padding: '8px', textAlign: 'left', fontWeight: 'bold' }}>Covariate</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>coef</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>exp(coef)</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>se(coef)</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>coef low 95%</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>coef upp 95%</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>exp(coef) low 95%</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>exp(coef) upp 95%</th>
        <th style={{ padding: '8px', textAlign: 'center', fontWeight: 'bold' }}>cmp to</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>z</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>p</th>
        <th style={{ padding: '8px', textAlign: 'right', fontWeight: 'bold' }}>-log2(p)</th>
      </tr>
    </thead>
    <tbody>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CARATER_ATENDIMENTO_URGENCIA</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-1.530013</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.216533</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.023494</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-1.576061</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-1.483965</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.206788</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.226737</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-65.122442</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000000</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>inf</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_Q</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.753743</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.470602</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.190935</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-1.127968</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.379519</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.323690</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.684191</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-3.947654</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000079</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>13.629238</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>FL_NEOPLASIA</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.614956</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.540665</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.058938</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.730472</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.499439</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.481681</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.606871</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-10.433907</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000000</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>82.252432</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>FL_DIABETES</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.568587</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.566325</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.118567</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.800974</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.336199</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.448892</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.714481</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-4.795479</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000002</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>19.233022</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_G</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.243897</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.783569</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.088339</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.417038</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.070755</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.658996</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.931690</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-2.760911</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.005764</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>7.438706</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>FL_INSUF_CARDIACA</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.243502</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.783878</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.082558</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.405314</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.081691</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.666768</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.921557</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-2.949457</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.003183</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>8.295250</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>FL_DOENCA_RENAL</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.231885</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.793037</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.080181</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.389037</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.074733</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.677709</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.927991</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-2.892022</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.003828</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>8.029301</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>SEXO_M</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.077686</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.925255</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.018754</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.114444</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.040929</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.891862</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.959898</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-4.142330</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000034</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>14.828091</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>IDADE</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.007210</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.992816</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000367</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.007929</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-0.006490</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.992102</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.993531</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>-19.637047</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000000</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>282.786388</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_R</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.108355</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.114443</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.047711</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.014843</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.201867</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.014954</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.223686</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>2.271063</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.023143</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>5.433268</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_n</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.157964</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.171124</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.032728</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.093819</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.222109</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.098361</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.248708</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>4.826613</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000001</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>19.457787</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_S</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.208270</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.231545</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.058707</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.093207</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.323333</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.097689</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.381725</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>3.547641</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000389</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>11.329063</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_C</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.220495</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.246694</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.071473</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.080411</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.360579</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.083733</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.434160</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>3.085027</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.002035</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>8.940517</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_K</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.224078</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.251169</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.040613</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.144478</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.303679</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.155436</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.354833</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>5.517370</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000000</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>24.792556</td>
      </tr>
      <tr style={{ borderBottom: '1px solid #e5e7eb' }}>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_T</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.291023</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.337795</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.039628</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.213353</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.368692</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.237821</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.445843</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>7.343838</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000000</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>42.131571</td>
      </tr>
      <tr>
        <td style={{ padding: '8px', fontWeight: 'bold' }}>CID_CAPITULO_V</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.074740</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>2.929232</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.238298</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.607684</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.541796</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>1.836175</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>4.672975</td>
        <td style={{ padding: '8px', textAlign: 'center' }}>0.0</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>4.510066</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>0.000006</td>
        <td style={{ padding: '8px', textAlign: 'right' }}>17.235407</td>
      </tr>
    </tbody>
  </table>
</div> 



### Discussão dos Resultados: O Que Impacta o Tempo de Internação?

Utilizando a análise multivariada (Regressão de Cox), conseguimos isolar o impacto real de cada variável na probabilidade de alta do paciente. Abaixo, traduzimos os coeficientes estatísticos (*Hazard Ratios*) em impactos operacionais claros.

#### O Grande Gargalo: Admissões via Urgência (HR ≈ 0.21)
A variável `CARATER_ATENDIMENTO_URGENCIA` provou ser o maior ofensor do giro de leitos.
* O que o dado diz: Um paciente admitido pela urgência tem uma probabilidade de alta diária 80% menor do que um paciente eletivo com características similares.
* Impacto no Negócio: Isso confirma estatisticamente que a imprevisibilidade da porta de entrada (Urgência) é a principal causadora de retenção de leitos. Processos desenhados para pacientes eletivos não funcionam aqui.

#### Complexidade Clínica como Redutor de Velocidade
O modelo quantificou exatamente o quanto condições crônicas "freiam" a alta:
* Oncologia (HR ≈ 0.54): Pacientes com neoplasia têm a velocidade de alta reduzida em 46%. É o fator clínico de maior peso, exigindo gestão de caso dedicada.
* Diabetes (HR ≈ 0.56): A presença de diabetes reduz a chance de alta em 44%, indicando que a estabilização clínica dessa comorbidade consome dias preciosos de internação.

#### O Peso da Idade na Ocupação
* Idade (HR ≈ 0.992): O envelhecimento atua como um fator de risco progressivo. A cada 10 anos a mais de vida, a agilidade na alta cai cerca de 8%. Embora pareça pouco isoladamente, em pacientes idosos esse efeito se acumula, tornando-os consumidores de longa permanência por natureza.

#### Oportunidades de Giro Rápido (HR > 1.0)
Identificamos perfis que aceleram o fluxo e podem ser otimizados:
* Causas Externas (Capítulo V): Apresentou o maior potencial de alta (HR ≈ 2.92). Pacientes desse grupo (ex: pequenos acidentes) giram quase 3x mais rápido que a média.
* Lesões e Traumas (Capítulo T): Com HR de 1.33, também indicam um perfil de resolução rápida.

Conclusão e Estratégia Sugerida:
A análise comprova que o Tempo de Permanência (LOS) é fortemente penalizado pela Origem (Urgência) e pela Carga de Doença.
Do ponto de vista de gestão, os dados sugerem que misturar esses perfis no mesmo fluxo gera ineficiência.


### 15. Persistência de Artefatos e Entrega do Produto de Dados

A etapa conclusiva do pipeline de dados consiste na materialização dos resultados para consumo operacional. O processo divide-se em duas vertentes de entrega: uma analítica (para humanos) e uma técnica (para sistemas).

Artefatos Gerados:

Relatório Analítico (`Relatorio_Previsoes.csv`):
    * Consolidação dos dados históricos enriquecidos com as inferências do modelo.
    * Colunas Adicionadas:
        * `PRED_DIAS_ESTADIA`: Estimativa de regressão (tempo de permanência).
        * `PROB_LONGA_PERMANENCIA`: Score de risco (0 a 100%) gerado pelo classificador.
        * `ALERTA_RISCO`: Flag binária baseada no *threshold* calibrado (0.35), indicando necessidade de intervenção imediata.
    * Finalidade: Alimentação direta de dashboards gerenciais (Power BI / Tableau) para uso da equipe do NIR (Núcleo Interno de Regulação).

Serialização de Modelos (`.pkl` / `.joblib`):
    * Exportação dos objetos treinados (o Regressor, o Classificador e os Pré-processadores como `LabelEncoders` e `Scalers`).
    * Finalidade: Garantir a portabilidade e reprodutibilidade do modelo. Estes arquivos binários permitem que o algoritmo seja embarcado em uma API (ex: via FastAPI ou Flask) para realizar predições em tempo real integradas ao Prontuário Eletrônico, sem a necessidade de re-treinamento a cada requisição.



## 16. Previsão de Tempo de Internação (LOS): Estratégia Baseada em Dados

## O Desafio de Negócio
A previsibilidade na ocupação de leitos é um dos maiores gargalos da gestão hospitalar. A incerteza sobre quando um paciente terá alta impacta desde o fluxo do Pronto Socorro até o planejamento de cirurgias eletivas.

Este projeto teve como objetivo responder à pergunta: É possível antecipar quais pacientes terão uma internação longa (alto custo) utilizando apenas os dados disponíveis na admissão?

## Estratégia e Metodologia
Para garantir que os dados refletissem a realidade operacional, realizei um tratamento rigoroso na base, corrigindo inconsistências temporais (erros de registro de datas) que poderiam enviesar a análise.

Adotei uma abordagem híbrida para resolver o problema:
Diagnóstico: Uso de Análise de Sobrevivência para entender o comportamento da alta ao longo do tempo.
Modelagem: Teste de dois cenários — tentar prever o dia exato da alta (Regressão) versus identificar o risco de longa permanência (Classificação).

## Principais Descobertas (Insights)

A análise estatística revelou os gargalos operacionais do hospital:
* O Fator Urgência: A origem do paciente é determinante. Pacientes admitidos pela emergência têm uma probabilidade de alta diária 80% menor do que pacientes eletivos. Isso valida estatisticamente a Urgência como o ponto crítico de retenção de leitos.
* Ofensores Clínicos: Identificamos que Neoplasias e Diabetes reduzem a velocidade de alta em cerca de 45%, independentemente da idade do paciente.
* Oportunidade de Giro: Diagnósticos de Causas Externas (ex: pequenos traumas) giram o leito quase 3 vezes mais rápido que a média.

## Resultados dos Modelos

### Decisão Estratégica: Classificação vs. Regressão
Ao comparar as abordagens, a Classificação de Risco mostrou-se superior para a tomada de decisão gerencial.

* O Limite da Regressão (Random Forest): O modelo conseguiu prever o tempo de internação com um erro médio de 3.6 dias. Embora útil para casos simples, ele perde precisão nos casos extremos (justamente os que mais impactam o custo).
* A Força da Classificação (XGBoost): Optamos por focar em identificar *quem* vai ficar mais de 8 dias. O modelo obteve uma excelente capacidade de discriminação (AUC-ROC de 0.87).

### Impacto Operacional
Ajustei o modelo para priorizar a segurança assistencial (Sensibilidade):
* O algoritmo conseguiu detectar 84% dos pacientes de longa permanência logo na entrada.
* Isso permite que a equipe de gestão de leitos atue preventivamente na maioria dos casos críticos, sem esperar o paciente "estourar" o tempo previsto.

### Transparência (White Box)
Para garantir a confiança da equipe médica, utilizei SHAP Values para explicar as decisões do modelo. A ferramenta confirmou que o algoritmo segue a lógica clínica: ele aprendeu que Idade Avançada, Instabilidade nos Sinais Vitais e Admissão via Urgência são os maiores vetores de risco.

## Conclusão
O estudo comprovou que dados básicos de admissão possuem alto poder preditivo. A implementação deste modelo de Triagem de Risco permite segregar fluxos: criar vias rápidas (*Fast Tracks*) para casos simples e alocar recursos de desospitalização precocemente para os casos complexos identificados pelo algoritmo.

### 17. Limitações e Oportunidades de Evolução

Embora o modelo seja funcional para a triagem inicial, é importante destacar pontos de atenção para sua implantação e evolução:

* Contexto Específico: O algoritmo aprendeu os padrões operacionais e clínicos de um único hospital. Para escalar a solução para outras unidades, seria necessário um re-treino para capturar as especificidades locais (processos e perfil demográfico).
* Profundidade dos Dados: O modelo utiliza apenas dados disponíveis na admissão. A inclusão de variáveis intra-hospitalares (exames laboratoriais, evolução de enfermagem ou scores) certamente aumentaria a precisão.
* Predição Estática ("Foto da Porta"): O risco é calculado apenas na entrada. Como o quadro clínico é dinâmico, uma evolução futura do projeto seria recalcular o score diariamente, ajustando a previsão conforme o paciente melhora ou piora.
* Validação em Produção: Utilizamos validação cruzada para garantir a robustez técnica. O próximo passo ideal seria um teste piloto (validação *Out-of-Time*) para monitorar a estabilidade do modelo frente a dados novos e sazonais antes do *rollout* total.
